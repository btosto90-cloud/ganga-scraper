def scrape_ml(marca, modelo='', paginas=5):
    listings = []
    ...
    for page in range(paginas):
        ...
        parsed = parse_ml_listings(html, marca)
        print(f"  ML {marca}{modelo_url} p{page+1}: {len(parsed)} | html_len: {len(html)} | has_preloaded: {'__PRELOADED_STATE__' in html} | has_wrapper: {'ui-search-result__wrapper' in html}")
